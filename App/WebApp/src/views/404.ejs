<%
// Der spezifische Style-Block, der in das Layout eingefügt wird.
// Da wir jetzt EJS verwenden, können wir CSS hier direkt im Style-Tag definieren,
// welches dann über die 'styles'-Variable an das Hauptlayout übergeben wird.
%>
<style>
    .not-found-container {
        text-align: center;
        margin-top: 50px;
        padding: 40px;
        border-radius: 12px;
        background-color: #ffffff;
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -2px rgba(0, 0, 0, 0.1);
    }
    .not-found-container h1 {
        font-size: 6em;
        margin-bottom: 0;
        color: #ef4444; /* Tailwind red-500 */
        font-weight: 800;
    }
    .not-found-container h2 {
        font-size: 2.5em;
        margin-top: 0;
        color: #1f2937; /* Tailwind gray-800 */
        font-weight: 600;
    }
    .not-found-container p {
        font-size: 1.1em;
        color: #6b7280; /* Tailwind gray-500 */
        margin-top: 15px;
    }
    .not-found-container a {
        color: #4f46e5; /* Tailwind indigo-600 */
        text-decoration: none;
        font-weight: 600;
        transition: color 0.15s;
    }
    .not-found-container a:hover {
        color: #3730a3; /* Tailwind indigo-800 */
        text-decoration: underline;
    }
</style>

<div class="not-found-container">
    <h1>404</h1>
    <h2>Seite nicht gefunden</h2>
    <p>Die angeforderte URL: <b><%= originalUrl %></b> konnte nicht gefunden werden.</p>
    <p><a href="/">Zurück zur Startseite</a></p>
</div>