<%
    currentPath = currentPath || '/';
%>
<nav class="p-4 space-y-1">
    
    <%- include('_menu_item', { 
        href: '/', 
        icon: 'fas fa-home', 
        text: 'Dashboard',
        currentPath: currentPath 
    }) %>

    <%- include('_menu_dropdown', {
        prefix: '/time-tracking', // Präfix für die Alpine-Logik
        icon: 'fa-solid fa-clock',
        text: 'Zeiterfassung',
        currentPath: currentPath,
        subitems: [
            // Unterpunkt 1: Nutzerliste (als String an das Dropdown übergeben)
            include('_menu_item', { 
                href: '/stamping', 
                icon: 'fa-solid fa-stamp', 
                text: 'Stempeln',
                currentPath: currentPath
            }),

            include('_menu_item', { 
                href: '/stamping-overview', 
                icon: 'fa-solid fa-calendar', 
                text: 'Monatsübersicht',
                currentPath: currentPath 
            })
        ]
    }) 
    %>
    
    <%- include('_menu_item', { 
        href: '/task-list', 
        icon: 'fa-solid fa-list-check', 
        text: 'Aufgaben',
        currentPath: currentPath 
    }) %>
    
    <%- include('_menu_dropdown', {
        prefix: '/user', // Präfix für die Alpine-Logik
        icon: 'fa-solid fa-users',
        text: 'Nutzerverwaltung',
        currentPath: currentPath,
        subitems: [
            // Unterpunkt 1: Nutzerliste (als String an das Dropdown übergeben)
            include('_menu_item', { 
                href: '/user-list', 
                icon: 'fa-solid fa-list-check', 
                text: 'Nutzerliste', 
                currentPath: currentPath 
            }),
            
            // Unterpunkt 2: Rollenverwaltung
            include('_menu_item', { 
                href: '/roles', 
                icon: 'fa-solid fa-user-tag', 
                text: 'Rollenverwaltung', 
                currentPath: currentPath 
            })
        ]
    }) %>
    
    <%- include('_menu_item', { 
        href: '/settings', 
        icon: 'fas fa-gear', 
        text: 'Einstellungen',
        currentPath: currentPath 
    }) %>
</nav>