// plattform-script.js

// Utility to check if the viewport is considered mobile

const MOBILE_BREAKPOINT = 768;

// Returns true if the viewport width is less than the mobile breakpoint
const isMobile = () => window.innerWidth < MOBILE_BREAKPOINT;
